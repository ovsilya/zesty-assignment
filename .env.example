# Google Cloud Platform Configuration
# Required: Your GCP project ID where BigQuery and Vertex AI are enabled
GOOGLE_CLOUD_PROJECT=zesty-481316

# LlamaParse API Key
# Required: Your LlamaParse API key for PDF parsing
# Get your key from: https://cloud.llamaindex.ai/
LLAMA_CLOUD_API_KEY=your-llamaparse-api-key

# Optional: Force reprocess all PDFs
# Set to "1", "true", or "yes" to reprocess all PDFs even if they were already processed
# Default: 0 (skip already processed PDFs)
FORCE_REPROCESS=0

# Google Cloud Authentication
# The code automatically uses zesty-*.json in the project root if available
# If you need to use a different service account key, uncomment and set:
# GOOGLE_APPLICATION_CREDENTIALS=path/to/service-account-key.json

# Alternative authentication methods:
# Option 2: Use gcloud CLI authentication (for local development)
# Run: gcloud auth application-default login
# No environment variable needed - uses Application Default Credentials

# Option 3: Use compute engine service account (if running on GCP)
# No environment variable needed - automatically uses attached service account
